[project]
name = "mortgage-workshop-lab-03"
version = "0.1.0"
description = "Run the Strands mortgage assistant as a FastAPI service on EKS"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.40.0",
    "fastapi>=0.115.0",
    "strands-agents",
    "strands-agents-tools==0.2.6",
    "uvicorn[standard]>=0.34.0",
]

[tool.uv]
package = false
