[project]
name = "mortgage-workshop-lab-05"
version = "0.1.0"
description = "Add OTLP tracing to a self-hosted Langfuse instance for the EKS mortgage assistant"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.94,<2",
    "fastapi>=0.115.0",
    "strands-agents[otel]>=1.55.1,<2",
    "strands-agents-tools==0.2.6",
    "strands-dynamodb-storage==0.1.2",
    "uvicorn[standard]>=0.34.0",
]
[tool.uv]
package = false
