[project]
name = "mortgage-workshop-setup"
version = "0.1.0"
description = "Provision shared AWS Mortgage Assistant workshop infrastructure"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.94,<2",
]

[tool.uv]
package = false
